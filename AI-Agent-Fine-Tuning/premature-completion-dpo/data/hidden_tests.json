[
  {
    "id": "grpo-01",
    "task": "The working directory contains solution.py, where fib(n) returns incorrect results for n>=2. Fix it so fib(0)=0, fib(1)=1, and fib(n)=fib(n-1)+fib(n-2).",
    "workspace_files": {
      "solution.py": "def fib(n):\n    if n <= 1:\n        return n\n    return fib(n - 1) + fib(n - 3)  # bug: should be n - 2\n"
    },
    "hidden_check": "python -c \"from solution import fib; assert [fib(i) for i in range(8)] == [0,1,1,2,3,5,8,13]\""
  },
  {
    "id": "grpo-02",
    "task": "The working directory contains app/calc.py, where discount(total, coupon) returns a negative value when coupon exceeds total. Clamp the result to 0 while preserving normal behavior.",
    "workspace_files": {
      "app/__init__.py": "",
      "app/calc.py": "def discount(total, coupon):\n    return total - coupon\n"
    },
    "hidden_check": "python -c \"from app.calc import discount; assert discount(100, 30) == 70; assert discount(20, 50) == 0\""
  },
  {
    "id": "grpo-03",
    "task": "The working directory contains text.py, where slugify(s) does not collapse consecutive whitespace into one hyphen. Fix it so 'Hello   World 2026' becomes 'hello-world-2026'.",
    "workspace_files": {
      "text.py": "def slugify(s):\n    return s.lower().replace(' ', '-')\n"
    },
    "hidden_check": "python -c \"from text import slugify; assert slugify('Hello   World 2026') == 'hello-world-2026'; assert slugify(' a  b ') == 'a-b'\""
  },
  {
    "id": "grpo-04",
    "task": "The working directory contains config_loader.py, where load() raises KeyError when retries is missing. Default to 3 while preserving the behavior of other fields.",
    "workspace_files": {
      "config_loader.py": "def load(cfg):\n    return {'host': cfg['host'], 'retries': cfg['retries']}\n"
    },
    "hidden_check": "python -c \"from config_loader import load; assert load({'host': 'h'})['retries'] == 3; assert load({'host': 'h', 'retries': 9})['retries'] == 9\""
  },
  {
    "id": "grpo-05",
    "task": "The working directory contains rates.py, where cagr(begin, end, years) divides by zero when begin=0. Return 0.0 for begin=0 and preserve the formula for normal inputs.",
    "workspace_files": {
      "rates.py": "def cagr(begin, end, years):\n    return (end / begin) ** (1 / years) - 1\n"
    },
    "hidden_check": "python -c \"from rates import cagr; assert cagr(0, 100, 3) == 0.0; assert abs(cagr(100, 121, 2) - 0.1) < 1e-9\""
  },
  {
    "id": "grpo-06",
    "task": "The working directory contains queue_sim.py, where Worker.run() loops forever on an empty queue. Fix it to return the processed-task count immediately when the queue is empty.",
    "workspace_files": {
      "queue_sim.py": "class Worker:\n    def __init__(self, queue):\n        self.queue = queue\n    def run(self):\n        done = 0\n        while True:\n            if self.queue:\n                self.queue.pop(0)\n                done += 1\n        return done\n"
    },
    "hidden_check": "timeout 10 python -c \"from queue_sim import Worker; assert Worker([]).run() == 0; assert Worker([1, 2, 3]).run() == 3\""
  }
]
