[pytest]
# Async tests are written as plain `async def test_*` without per-test markers.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = .
python_files = test_*.py
# hello.py / test_script.py are demo fixtures for the tools, not test modules.
addopts = --ignore=test_script.py
