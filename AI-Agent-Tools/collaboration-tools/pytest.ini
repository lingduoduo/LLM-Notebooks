[pytest]
# `async def` tests appear across this suite, some marked with
# @pytest.mark.asyncio and some not. Without asyncio_mode=auto, the unmarked
# ones are collected but never awaited, and pytest reports:
#   Failed: async def functions are not natively supported
asyncio_mode = auto
testpaths = .
